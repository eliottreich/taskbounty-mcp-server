{
  "name": "taskbounty-mcp-server",
  "mcpName": "io.github.eliottreich/taskbounty-mcp-server",
  "version": "0.3.2",
  "license": "MIT",
  "description": "MCP server for TaskBounty. Post and fund GitHub bug bounties, enable Autopilot on your repos, and let your AI agent find and solve open bounties (fixes ship with a regression test, verified in a sandbox). Funded in USD, paid in USDC, ETH, or BTC. TaskBounty also offers Coverage Uplift, a flat-price test-coverage service ordered at task-bounty.com/coverage.",
  "keywords": [
    "mcp",
    "model-context-protocol",
    "claude",
    "ai-agents",
    "bounties",
    "test-coverage",
    "code-coverage",
    "autopilot",
    "github",
    "developer-tools"
  ],
  "homepage": "https://www.task-bounty.com",
  "repository": {
    "type": "git",
    "url": "https://github.com/eliottreich/taskbounty-mcp-server.git"
  },
  "bugs": {
    "url": "https://github.com/eliottreich/taskbounty-mcp-server/issues"
  },
  "type": "module",
  "bin": {
    "taskbounty-mcp-server": "build/index.js"
  },
  "main": "build/index.js",
  "files": [
    "build",
    "README.md"
  ],
  "scripts": {
    "build": "tsc && chmod +x build/index.js",
    "test": "tsc && node --test test/*.test.mjs",
    "start": "node build/index.js",
    "test": "npm run build && tsc -p tsconfig.test.json && node --test \".test-build/**/*.test.js\"",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "@types/node": "^25.9.1"
  },
  "engines": {
    "node": ">=20"
  }
}
